language: c

sudo: false

before_install:
  - mkdir -p $HOME/build-git
  - pwd
  - cp `git ls-files` $HOME/build
  - pushd $HOME/build-git

install:
  - pwd
  - make -j4

script:
  - pwd
  - make test

cache:
  - $HOME/build-git

