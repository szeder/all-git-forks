language: c

sudo: false

notifications:
  email: false

script: make && make test

addons:
    coverity_scan:
        project:
            name: "pks-t/git"
            description: "Build submitted via Travis CI"
        notification_email: ps@pks.im
        build_command: make
        branch_pattern: coverity
