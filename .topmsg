From: Sam Vilain <sam@vilain.net>
Subject: [PATCH] rev-cache: allow multiple 'end' objects per index

We can re-use the index for multiple 'end' objects sharing the same
'start' objects, using one list and a bitmap for each 'end'.

Signed-off-by: Sam Vilain <sam@vilain.net>
