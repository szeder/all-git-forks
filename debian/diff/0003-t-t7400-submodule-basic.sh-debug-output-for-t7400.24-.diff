From a067071112a33f16e19d195ec989e2c5d6e58090 Mon Sep 17 00:00:00 2001
From: Gerrit Pape <pape@smarden.org>
Date: Fri, 23 Apr 2010 12:10:03 +0000
Subject: [PATCH 3/3] t/t7400-submodule-basic.sh: debug output for t7400.24 'update --init'

---
 t/t7400-submodule-basic.sh |    6 +++++-
 1 files changed, 5 insertions(+), 1 deletions(-)

diff --git a/t/t7400-submodule-basic.sh b/t/t7400-submodule-basic.sh
index 1a4dc5f..367ddfb 100755
--- a/t/t7400-submodule-basic.sh
+++ b/t/t7400-submodule-basic.sh
@@ -255,18 +255,22 @@ test_expect_success 'apply submodule diff' '
 	test -z "$D"
 '
 
+set -x
 test_expect_success 'update --init' '
 
 	mv init init2 &&
 	git config -f .gitmodules submodule.example.url "$(pwd)/init2" &&
-	git config --remove-section submodule.example
+	git config --remove-section submodule.example &&
 	git submodule update init > update.out &&
+	grep ^ update.out
 	grep "not initialized" update.out &&
 	test ! -d init/.git &&
 	git submodule update --init init &&
 	test -d init/.git
 
 '
+set +x
+find . -type f | grep -v .git | xargs grep ^ || :
 
 test_expect_success 'do not add files from a submodule' '
 
-- 
1.7.0.3

